---
title: Lite Query
description: The lightweight, single-file data loading library for React.
template: splash
hero:
  tagline: The lightweight, single-file data loading library for React.
  # image:
  #   file: ../../assets/houston.webp
  actions:
    - text: Read the docs
      link: /guides/example/
      icon: right-arrow
      variant: primary
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Basic Usage

```jsx
import { useQuery } from './utils/lite-query';

const { data, error, isLoading } = useQuery({
	queryKey: 'posts',
	queryFn: () => fetch('https://jsonplaceholder.typicode.com/posts').then(res => res.json())
});

if (isLoading) return 'Loading...';
if (error) return `Error: ${error.message}`;
```

## How to Install

Simply copy [this file](https://github.com/Orva-Studio/lite-query/blob/main/src/index.ts) and put it anywhere in your project.

If you'd like to install it **as a package**, that will be coming soon...


## Features
Lite Query address all the data fetching issues raised in the [React Documentation](https://react.dev/reference/react/useEffect#what-are-good-alternatives-to-data-fetching-in-effects) caused by the useEffect hook such as:

<CardGrid stagger>
	<Card title="Data Caching" icon="star">
		Simple caching system preventing multiple requests for the same data.
	</Card>
	<Card title="Prefetching" icon="star">
		Add Markdown or MDX files to `src/content/docs` to create new pages.
	</Card>
	<Card title="SWR" icon="star">
		Edit your `sidebar` and other config in `astro.config.mjs`.
	</Card>
	<Card title="And much more..." icon="star">
		Learn more about all the features in the [Lite Query Docs](https://starlight.astro.build/).
	</Card>
</CardGrid>
